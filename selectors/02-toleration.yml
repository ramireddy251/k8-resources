apiVersion: v1
kind: Pod
metadata:
  name: nginx-tolerate
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
  nodeSelector:
    project: roboshop  
  tolerations: 
  - key: "project"
    operator: "Equal"
    value: "roboshop"
    effect: "NoExecute"  